# Ro-Graph
Currently just a weird programing languge made to do math stuff mostly just for the hell of it.

anyway syntax guide

## strings
string work simlary to other languges but are defined with ```( your string )```.

## functions
functions are the first word in your program a current list at time of writing.


```luau
ADD VALUE; -- adds active and VALUE

SET VALUE; -- sets active to VALUE

WRITE STRING; -- writes to CACHE
```

## SpecialValues
Perderminated Values

```luau
PI; -- 3.14

READ(STRING); -- reads from CACHE

```



